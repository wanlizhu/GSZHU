add_library(Render STATIC "${RSD}/dummy.cpp")
if (NOT GE_OUTPUT_WITH_CONFIG_SUBDIR)
    get_target_property(_outdir_ Render RUNTIME_OUTPUT_DIRECTORY)
    get_target_property(_libdir_ Render LIBRARY_OUTPUT_DIRECTORY)
    set_target_properties(Render PROPERTIES RUNTIME_OUTPUT_DIRECTORY_DEBUG          "${_outdir_}")
    set_target_properties(Render PROPERTIES RUNTIME_OUTPUT_DIRECTORY_RELEASE        "${_outdir_}")
    set_target_properties(Render PROPERTIES RUNTIME_OUTPUT_DIRECTORY_MINISIZEREL    "${_outdir_}")
    set_target_properties(Render PROPERTIES RUNTIME_OUTPUT_DIRECTORY_RELWITHDEBINFO "${_outdir_}")
    set_target_properties(Render PROPERTIES LIBRARY_OUTPUT_DIRECTORY_DEBUG          "${_libdir_}")
    set_target_properties(Render PROPERTIES LIBRARY_OUTPUT_DIRECTORY_RELEASE        "${_libdir_}")
    set_target_properties(Render PROPERTIES LIBRARY_OUTPUT_DIRECTORY_MINISIZEREL    "${_libdir_}")
    set_target_properties(Render PROPERTIES LIBRARY_OUTPUT_DIRECTORY_RELWITHDEBINFO "${_libdir_}")
endif()

set(CSD "${CMAKE_CURRENT_SOURCE_DIR}")
file(GLOB_RECURSE _headers_    "${CSD}/include/*.h" "${CSD}/include/*.hpp")
file(GLOB_RECURSE _sources_    "${CSD}/source/*.c"  "${CSD}/source/*.cpp")
source_group(TREE "${CSD}/include"  PREFIX "Header Files" FILES ${_headers_})
source_group(TREE "${CSD}/source"   PREFIX "Source Files" FILES ${_sources_})
target_sources(Render PRIVATE ${_headers_} ${_sources_})
target_link_libraries(Render PRIVATE Utility)
target_link_libraries(Render PRIVATE CGAL)
target_link_libraries(Render PRIVATE GIH_Vulkan)
target_link_libraries(Render PRIVATE CONAN_PKG::glfw)
target_link_libraries(Render PRIVATE CONAN_PKG::imgui)
target_link_libraries(Render PRIVATE CONAN_PKG::Assimp)
target_link_libraries(Render PRIVATE CONAN_PKG::jsonformoderncpp)
target_link_libraries(Render PRIVATE CONAN_PKG::stb)

target_include_directories(Render PUBLIC "${CSD}/include" PRIVATE "${CSD}/source")

get_target_property(_outdir_ Render LIBRARY_OUTPUT_DIRECTORY)
message(STATUS "Target: Render -> ${_outdir_}" )