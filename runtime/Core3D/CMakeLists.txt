add_library(Core3D STATIC "${RSD}/dummy.cpp")
if (NOT GE_OUTPUT_WITH_CONFIG_SUBDIR)
    get_target_property(_outdir_ Core3D RUNTIME_OUTPUT_DIRECTORY)
    get_target_property(_libdir_ Core3D LIBRARY_OUTPUT_DIRECTORY)
    set_target_properties(Core3D PROPERTIES RUNTIME_OUTPUT_DIRECTORY_DEBUG          "${_outdir_}")
    set_target_properties(Core3D PROPERTIES RUNTIME_OUTPUT_DIRECTORY_RELEASE        "${_outdir_}")
    set_target_properties(Core3D PROPERTIES RUNTIME_OUTPUT_DIRECTORY_MINISIZEREL    "${_outdir_}")
    set_target_properties(Core3D PROPERTIES RUNTIME_OUTPUT_DIRECTORY_RELWITHDEBINFO "${_outdir_}")
    set_target_properties(Core3D PROPERTIES LIBRARY_OUTPUT_DIRECTORY_DEBUG          "${_libdir_}")
    set_target_properties(Core3D PROPERTIES LIBRARY_OUTPUT_DIRECTORY_RELEASE        "${_libdir_}")
    set_target_properties(Core3D PROPERTIES LIBRARY_OUTPUT_DIRECTORY_MINISIZEREL    "${_libdir_}")
    set_target_properties(Core3D PROPERTIES LIBRARY_OUTPUT_DIRECTORY_RELWITHDEBINFO "${_libdir_}")
endif()

set(CSD "${CMAKE_CURRENT_SOURCE_DIR}")
file(GLOB_RECURSE _headers_    "${CSD}/include/*.h" "${CSD}/include/*.hpp")
file(GLOB_RECURSE _sources_    "${CSD}/source/*.c"  "${CSD}/source/*.cpp")
if (APPLE)
    file(GLOB_RECURSE _objc_    "${CSD}/source/*.m"  "${CSD}/source/*.mm")
    list(APPEND _sources_ ${_objc_})
endif()

source_group(TREE "${CSD}/include"  PREFIX "Header Files" FILES ${_headers_})
source_group(TREE "${CSD}/source"   PREFIX "Source Files" FILES ${_sources_})
target_sources(Core3D PRIVATE ${_headers_} ${_sources_})

target_include_directories(Core3D PUBLIC "${CSD}/include" PRIVATE "${CSD}/source")
target_link_libraries(Core3D PRIVATE CONAN_PKG::Assimp)
target_link_libraries(Core3D PRIVATE CONAN_PKG::Eigen3)
target_compile_definitions(Core3D PRIVATE "_UNICODE" "UNICODE")

get_target_property(_outdir_ Core3D LIBRARY_OUTPUT_DIRECTORY)
message(STATUS "Target: Core3D -> ${_outdir_}" )