#pragma once

#include "math/Vector.h"

namespace NTGS {
    template<template<typename T> class _QUAT, typename T>
    struct QuaternionOperators {
    private:
        using QUAT = _QUAT<T>;

    public:

    };
}